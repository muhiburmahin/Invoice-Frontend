import { PricingPage } from "@/components/modules/pricing/PricingPage";

export const metadata = {
  title: "Pricing | Invoice SaaS",
  description: "Free, Pro, and Enterprise plans with transparent limits. Start free, upgrade when you grow.",
};

export default function Page() {
  return <PricingPage />;
}
