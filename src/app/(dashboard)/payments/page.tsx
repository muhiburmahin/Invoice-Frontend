import { PaymentsPage } from "@/components/modules/payments/PaymentsPage";

export const metadata = {
  title: "Payments",
  description: "Track and record invoice payments.",
};

export default function Page() {
  return <PaymentsPage />;
}
